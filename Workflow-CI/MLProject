name: diabetes_ml_project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "diabetes_preprocessing.csv"}
      scaler_path: {type: str, default: "scaler.pkl"}
    command: "python modelling.py --data_path {data_path} --scaler_path {scaler_path}"
